generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Producto {
  id        Int      @id @default(autoincrement())
  producto  String
  stock     Int
  precio    Float
  categoria String
  fecha     DateTime @default(now())
}
